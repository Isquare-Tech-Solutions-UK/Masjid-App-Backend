server {
    listen 80;
    server_name 3.6.40.125.nip.io;

    # Allow Certbot challenges (for renewal)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all other HTTP traffic to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name 3.6.40.125.nip.io;

    ssl_certificate /etc/letsencrypt/live/3.6.40.125.nip.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/3.6.40.125.nip.io/privkey.pem;

    location / {
        proxy_pass http://app:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
